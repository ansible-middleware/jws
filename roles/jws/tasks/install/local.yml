---
- name: Check download archive path
  ansible.builtin.stat:
    path: "{{ jws_install_dir }}/{{ zipfile_name }}"
  register: archive_path

- name: Check download native archive path
  ansible.builtin.stat:
    path: "{{ jws_install_dir }}/{{ jws_native_zipfile }}"
  when:
    - jws_native
    - jws_native_zipfile | length > 0
  register: native_archive_path

- name: Check local download archive path
  ansible.builtin.stat:
    path: "{{ lookup('env', 'PWD') }}"
  register: local_path
  delegate_to: localhost

- name: Check downloaded archive
  ansible.builtin.stat:
    path: "{{ local_path.stat.path }}/{{ zipfile_name }}"
  register: local_archive_path
  delegate_to: localhost

- name: Check downloaded native archive
  ansible.builtin.stat:
    path: "{{ local_path.stat.path }}/{{ jws_native_zipfile }}"
  register: local_native_archive_path
  when:
    - jws_native
    - jws_native_zipfile | length > 0
  delegate_to: localhost

- name: Copy archive to target nodes
  ansible.builtin.copy:
    src: "{{ local_path.stat.path }}/{{ zipfile_name }}"
    dest: "{{ jws_install_dir }}/{{ zipfile_name }}"
    owner: "{{ tomcat.user }}"
    group: "{{ tomcat.group }}"
    mode: 0640
  register: new_version_downloaded
  when:
    - not archive_path.stat.exists
    - local_archive_path.stat is defined
    - local_archive_path.stat.exists
  become: yes

- name: Copy native archive to target nodes
  ansible.builtin.copy:
    src: "{{ local_path.stat.path }}/{{ jws_native_zipfile }}"
    dest: "{{ jws_install_dir }}/{{ jws_native_zipfile }}"
    owner: "{{ tomcat.user }}"
    group: "{{ tomcat.group }}"
    mode: 0640
  register: new_native_version_downloaded
  when:
    - jws_native is defined and jws_native_zipfile | length > 0
    - not native_archive_path.stat.exists
    - local_native_archive_path.stat is defined
    - local_native_archive_path.stat.exists
    - jws_native
    - jws_native_zipfile | length > 0
  become: yes
